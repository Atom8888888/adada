=========================

คำสั่ง

!topup | setupหน้าต่างโดนท

!data | เช็คเเงินที่ได้รับ

!check | เช็คการตั้งค่าบอท

=========================

วิธีตั้งค่าบอท

ตั้งค่าบอทในไฟล์ config.json

addrole คือ ให้ยศที่ตั้งค่าไว้ใน roleid เมื่อโดเนทเงินถึงจำนวนที่ตั้งค่าไว้ใน money 

on / เปิด

off / ปิด

=========================

รันบอท

ลบโมดูล discord หรือ discord.py ก่อนใช้งาน

pip install nextcord

py topup.py

=========================